machine:
  services:
    - docker
test:
  override:
    - go test -v -race ./...
    - docker build -f docker/Dockerfile --rm=false -t test-alpine-image .
    - docker build -f docker/Dockerfile.arm --rm=false -t test-arm-image .